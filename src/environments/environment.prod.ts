import { AbstractEnvironment } from './abstract-environment';

export const environment: AbstractEnvironment = {
  production: true,
  apiRoot: ' https://firestore.googleapis.com/v1/projects/cas-scheduler/databases/(default)/documents/lectures'
};
